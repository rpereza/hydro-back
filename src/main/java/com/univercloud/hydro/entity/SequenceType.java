package com.univercloud.hydro.entity;

/**
 * Tipo de secuencia de consecutivos por año y corporación.
 * Cada tipo tiene su propia secuencia independiente (p. ej. descargas y facturas).
 */
public enum SequenceType {
    DISCHARGE,
    INVOICE
}
